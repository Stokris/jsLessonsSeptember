/*
Parameters

parameters are like empty storage boxes we can name whatever
*/

function pet(animal){
    console.log(`i have a ${animal} for a pet.`);
}

pet('cat');

function myName(fName, lName){
    console.log(`Hello, my name is ${fName} ${lName}`);
}

myName('Kristen', 'Stokes');